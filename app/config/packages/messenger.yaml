framework:
    messenger:
        # Uncomment this (and the failed transport below) to send failed messages to this transport for later handling.
        # failure_transport: failed

        transports:
            # https://symfony.com/doc/current/messenger.html#transport-configuration
            # async: '%env(MESSENGER_TRANSPORT_DSN)%'
            # failed: 'doctrine://default?queue_name=failed'
            # sync: 'sync://'
            async: '%env(MESSENGER_TRANSPORT_DSN)%'

#            notification_queue:
#                serializer: messenger.transport.symfony_serializer
#                dsn: '%env(DSN_ASYNC_TRANSPORT)%'
#                options:
#                    exchange:
#                        name: worker-notification
#                        type: direct
#                        default_publish_routing_key: main
#                    queues:
#                        notification_queue:
#                            binding_keys: [ main ]

        routing:
            # Route your messages to the transports
            # 'App\Message\YourMessage': async
            # 'App\Message\Notification\SendNotificationMessageInterface': [ notification_queue ]
            'App\Message\Notification\SendNotificationMessageInterface': async

# when@test:
#    framework:
#        messenger:
#            transports:
#                # replace with your transport name here (e.g., my_transport: 'in-memory://')
#                # For more Messenger testing tools, see https://github.com/zenstruck/messenger-test
#                async: 'in-memory://'
